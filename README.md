# Former22 (middleware)

## Archive Mode
Archive allow return only usefull informations by years

It can to be enabled by environment variable
```
ARCHIVE_MODE=0
```
0 = current year - 1\
1 = older years
